# MAX-009 10 分钟演示脚本

> ⚠️ **ARCHIVED 2026-08-18 — 不是现行依据**
> 归档理由：零售 MVP（MAX-001~009）转型执行的过程记录，全部工单已 ACCEPTED；未完成的延后治理项见 docs/execution/精简MVP路线与延后治理清单.md
> 现行入口：`docs/AI_文档索引与现行决策.md`

状态：`ACCEPTED / GO`；固定上下文与证据见 [`reports/MAX-009.md`](reports/MAX-009.md)，最终评审见 [`reviews/MAX-009_review_8.md`](reviews/MAX-009_review_8.md)。脚本不包含密码/token。

1. **0:00–1:00 登录/兼容**：登录法人账号，确认“分析与决策”组第一项为经营脉搏；打开 `/`、`/performance`、`/contracts`、`/reports`、`/monthly-closing`，说明旧功能保留。
2. **1:00–2:00 Pulse**：进入 `/operating-pulse`，复用 completed fixed dataset；展示 simulated/Working、dataset/source/as-of/window、420/420、rank1 `SIM-2853D953-007`、high/3.02、occupancy `10.08pp`。
3. **2:00–4:00 Store360**：点击 rank1，展示目标身份、同 brand+region+currency peers、current/comparison、trend、三组变化 bridge/evidence；强调这是观察信号，不是根因。
4. **4:00–6:00 Scenario**：horizon=12，七项 delta 仅 labor `-10%`；计算后展示 labor `14,193.26→12,773.93 CNY`、月度 `1,419.33`、12 月 `17,031.91`、bridge residual `0.00`，展开证据与公式并说明不进入 IFRS16/Official。
5. **6:00–7:30 AI**：从三页进入 `/ai-chat`；Starter/请求使用 `retail_operations@v1`，真实 trace 顺序为 pulse → diagnostics → scenario，模型 deterministic-fallback。展示 simulated/dataset/source/evidence/formula、proposal、Owner/Due `—`、`formal_execution=false`、`business_write=false`，点击 source 回站内页面。
6. **7:30–8:30 Confirm**：返回 Scenario，修改假设后展示 stale/保存 disabled；重新计算，填写标题/计划动作，先截图确认 Modal，再 OK，展示真实 `open` action；同 key replay ID 不变。
7. **8:30–9:20 负向/隔离**：LE002 请求 LE001 门店得到不可见/空 envelope；production dataset 为 null；缺事实/invalid rate 得 needs-input/422 evidence，不产生 action/proposal。Prompt injection 只返回只读边界；详细 status/reason/trace 见 `r3-negative-agent.json` 与 `r3-cross-scope.json`。
8. **9:20–10:00 Mobile**：390×844 打开 AI Drawer，Tab/Enter/Space 选择会话、active/normal More/delete 可见、Escape 回焦点；四页 `scrollWidth=clientWidth=390`。R3 截图索引如实区分 browser inner viewport 与内容区位图像素。最后说明本票 fixture 已精确清理 residual=0。

Review 4 复现补充（历史记录）：在同一 `/operating-pulse` tab 连续点 28天→7天，`r4-race-browser.json` 仅保留 normalized Core endpoint、request key、click Promise status 和最终 7天 DOM；它不是完整浏览器网络证据，当前有效 race 证据见 R5。逐例查看四份 `r4-negative-*.json` 的原始 trace/events 与 `0→0` 写入计数；以 LE001 先读取 `r4-cross-scope.json` 中的 action/run/session/artifact，再以 LE002 重放相同目标验证空 envelope/404。R4 screenshot `r4-ai-proposal-1280x720.png` 如实标注为 1280×720，不宣称 1440×900。

Review 5 复现补充：查看 `r5-race-gate.json` 的同页最终 7 天 DOM 与实际页面 request gate deferred 测试（新响应先提交、旧响应后提交被拒）；查看 `r5-negative-agent.json` 中三例 raw assistant/event 的 reason、evidence、confidence 和底层 tool error；查看 `r5-residual.json` 的完整 UUID/username/key selector 与 residual=0。Browser 控制面没有 HTTP timing 导出，脚本不把 click Promise 当作网络时序。

Review 7 收口补充：no-facts 证据来自普通法人 scope 的 deterministic Response/ProjectResult 与 `SideEffects=false`、零 proposal/artifact；scope denial 保持原 reason。R4 四个唯一 username/refresh exact selector 与可恢复 UUID selector residual=0；等待 Review 8。

失败兜底：任一跨法人、IFRS16/Official 触达、404/白屏、stale 保存、移动溢出或关键按钮不可达，停止并将发布结论保持 `NO-GO`。
