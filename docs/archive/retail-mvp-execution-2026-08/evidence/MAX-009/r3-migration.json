{
  "migration": "db/migrations/040_agent_artifact_retail_proposal.sql",
  "previous_migration_023": "retail_action_proposal absent; old artifact types plus generic retained",
  "existing_volume": {
    "command": "docker exec -i lease-postgres psql -v ON_ERROR_STOP=1 -U lease -d lease < db/migrations/040_agent_artifact_retail_proposal.sql",
    "constraint": "CHECK (artifact_type = ANY (contract_draft,payment_schedule_draft,event_draft,audit_pack,data_quality_issue_list,report_explanation,monthly_close_blockers,retail_action_proposal,generic))",
    "transaction": "retail_action_proposal INSERT + generic INSERT both succeeded, then ROLLBACK"
  },
  "fresh_init": {
    "container": "max009r3-fresh-postgres",
    "volume": "max009r3_fresh_init",
    "command": "docker run -d --name max009r3-fresh-postgres ... -v max009r3_fresh_init:/var/lib/postgresql/data -v db/init:/docker-entrypoint-initdb.d:ro postgres:16-alpine",
    "result": "fresh init ready; final ai_chat_artifacts constraint includes retail_action_proposal",
    "teardown": "docker rm -f max009r3-fresh-postgres && docker volume rm max009r3_fresh_init",
    "original_volume_touched": false
  }
}
