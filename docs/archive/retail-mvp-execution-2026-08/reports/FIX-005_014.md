# FIX-005 ~ FIX-013 + DIAG-001 交付报告

> ⚠️ **ARCHIVED 2026-08-18 — 不是现行依据**
> 归档理由：零售 MVP（MAX-001~009）转型执行的过程记录，全部工单已 ACCEPTED；未完成的延后治理项见 docs/execution/精简MVP路线与延后治理清单.md
> 现行入口：`docs/AI_文档索引与现行决策.md`

提交人：ZCode（全栈）
提交时间：2026-08-15
分支：`fix/workstation-shell-005-014`（已推送，**未合 main**）
基线：`main @ 11895db`（HOME-004 已合入）
11 个独立 commit（01–11，对应各票）；**未做任何「外观变化清单」**——本报告只列结构事实与实测数值。

---

## 0. 执行方式说明

- **A 组（FIX-005~007）与 B 组（FIX-008~014）+ DIAG-001 全部完成**，无顺延。
- 复用路径沿用 HOME-004 的裁决：首页内升级、不抽代码、`/ai-chat` 零改动（本批 FIX-006/007/013 均遵守）。
- 浏览器实测用 headless Chrome 连 worktree 的 dev server（`localhost:3001`），真实登录 admin_user，全部数值 `getBoundingClientRect` / `getComputedStyle` 机械获取。

---

## 1. FIX-005 右栏 300px 压坏金额卡（回归修复）

**选型**：右栏 300→**330px**（任务书方案 1）。不选方案 2（MoneyKPICard 降档字号）的理由：那是动公共 dashboard 组件，影响所有使用它的页面；右栏宽度是首页布局参数，且 330px 是实测「值行保持单行的最小宽度」。

**实测（1440×900，headless Chrome，注入与 MoneyKPICard 完全同款的 26px 值行「1.28亿 CNY」）**：

| 右栏宽度 | 卡宽 | 值行高度 | 折行？ |
|---|---|---|---|
| 300（改动前） | 142px | **82px** | 是（"1.28亿" 与 "CNY" 拆行） |
| 310 | 147px | 82px | 是 |
| 320 | 152px | **82px** | 是（「1.28亿 CNY」仍折） |
| **330（改动后）** | **157px** | **41px** | **否** |
| 340 | 162px | 41px | 否 |

改动前 300px 数据与 Reviewer 实测（82px/300px、41px/340px）**完全吻合**。330px 是让当前 seed 真实值保持单行的最小宽度（320 仍折）。

**L8 连带影响（任务书拦坑 1）**：`.home-grid` 改 `minmax(640px, 1fr) 330px` 后，1440 下中栏实测 **750px ≥ 640** ✓（改动前 780px）；1280 下中栏 590px、右栏 330px 可用 ✓。两档都给了实测值。

**验收**：`chatLayout.test.ts` 新增 FIX-005 describe（断言 330px + 值行不折行许可）；命令输出见 §12。

---

## 2. FIX-006 简报带两个一模一样的展开控件

**根因**：`BriefBand` 自己渲染 toggle（取 `trust.expand`）后，受控的 `<DataTrustBar>` 仍渲染自带的同名 toggle。复现（改动前）：`[...band.querySelectorAll('button')].map(b => b.textContent)` → `["▶查看全部口径", "查看全部口径"]`，band 高 **132.5px**（Reviewer 实测；我环境一致）。

**修复**（二选一，选「受控即隐藏」并在注释写明理由）：
- `DataTrustBar`：`controlled`（`expanded`/`onToggle` 任一提供）时不渲染自身 toggle——受控的意图就是外部统一管理展开，两个同文案按钮必居其一。非受控调用方（pulse / store-360 / scenario-workbench / RetailAIDrawer / BriefView）行为不变，既有断言未改弱（`DataTrustBar.test.tsx` 新增受控/非受控两条断言，原 5 条原样保留）。
- 顺带发现并修复收起态超高的另一半：信任条 summary 的「对比 coverage」段（约 210px）把「决策就绪」挤出第一行。§2.1 收起态清单（分类·覆盖率·decision-ready·关注数·展开入口）**不含对比段**——给 `DataTrustBar` 加可选 `hideComparison`，Band 收起时隐藏（展开时显示，对比属全量口径）。

**实测（改动后，1440×900）**：`buttons: ["▶查看全部口径"]`（**1 个**）；band 收起态高度 **95px ≤ 96** ✓。

**验收**：`BriefBand.test.tsx` 断言 ready 态 `.home-brief-band` 内 `button` 数量 === 1 且无 `data-trust-bar-toggle`；`DataTrustBar.test.tsx` 断言非受控有 toggle、受控无 toggle。

---

## 3. FIX-007 输入框并没有「常驻底部」

**根因**（两处）：
1. `.home-chat-column` 无高度约束、body 无 overflow → composer 随页面滚（Reviewer 实测 composer bottom 1218 > 视口 900，一致）。
2. 顺带发现：`BriefColumn` 的 `scrollIntoView` 在**无消息时**也触发，页面加载即把内容往下推（`.app-content` 被滚 253px）。

**修复**：
- `.home-chat-column`：`height: calc(100dvh - 124px)`（60 header + 32 content padding + 32 sticky offset）+ `position: sticky; top: 32px`；`.home-chat-body`：`flex: 1; min-height: 0; overflow-y: auto`；composer 在滚动容器外（flex 列尾）。
- `.home-grid` 的 `align-items: start` → 默认 stretch：中栏网格轨道高 = 两列最大者，sticky 才有移动空间（此前 sticky 因父高 = 自身而失效）。
- 自动滚动只在 `messages.length > 0` 时执行。

**实测（改动后，1440×900，简报带展开 + 注入 14 条消息）**：

| 指标 | 值 |
|---|---|
| composer `getBoundingClientRect().bottom` | **900 = 视口高 900** ✓ |
| composer top | 864（视口内可见） |
| `.home-chat-body` scrollHeight / clientHeight | 1366 / 645（**内部滚动** ✓） |
| 简报带展开态高度 | 95px |

页面滚动（`.app-content` scrollTop 变化）后 composer bottom 恒定 900（sticky 生效）。

**验收**：`chatLayout.test.ts` FIX-007 describe（column 有 height calc + sticky、body overflow-y auto + flex:1、composer 在 body 外）。

---

## 4. FIX-008 chat 细节三连（一个 commit）

| # | 问题 | 修复 | 实测/断言 |
|---|---|---|---|
| 1 | `.home-chat-starter-chip { border-radius: 9999; }` 无单位，声明被丢弃（computed = 6px） | `9999px` | `getComputedStyle(chip).borderRadius === "9999px"` ✓（FIX-010 测量顺带验证） |
| 2 | 用户消息头像方向与 `/ai-chat` 相反（首页两角色都先渲染 Avatar，靠 justify-content） | `.home-msg.is-user { flex-direction: row-reverse }`（头像右、气泡左，与 `/ai-chat` 的 row-reverse 一致） | CSS 断言 |
| 3 | 建议提问只 `setInput`，不发送（`/ai-chat` 是直接发送） | 抽 `sendText(text)`，starter `onClick={() => sendText(...)}` | `chatLayout.test.ts` FIX-008 describe（`sendText(t(key, language))` 且无 `askStarter`） |

**验收**：`chatLayout.test.ts` FIX-008 describe 三条断言。

---

## 5. FIX-009 卡片之间没有间隙（全站审计）

**根因**：`<Spin>` 插入 `.ant-spin-nested-loading > .ant-spin-container` 两层 div，父级 gap 只作用于直接子节点，Spin 内多块之间 0 间距（Reviewer 实测 0px，与结构一致）。

**修复**（作用域限定，未写全局规则）：
```css
.home-right-stack .ant-spin-container { display:flex; flex-direction:column; gap:24px; }
.home-work-focus .ant-spin-container  { display:flex; flex-direction:column; gap:16px; }
```

**实测（改动后，1440×900，右栏 4 块相邻间距，`getBoundingClientRect` 相邻块 bottom→top）**：

| 相邻区块 | 间距 |
|---|---|
| 工作队列卡 → KPI 卡组 | **24px** |
| KPI 卡组 → 结账就绪度卡 | **24px** |
| 结账就绪度卡 → 未来关键日期卡 | **24px** |

**全站 `<Spin spinning>` 审计表**（逐个核对）：

| 文件:行 | 包住兄弟数 | 父级 gap 依赖 | 是否受影响 |
|---|---|---|---|
| `settings/page.tsx:497` | 1（Space 控件组） | 无 | 不受影响 |
| `settings/page.tsx:560` | 1（Row 卡组） | 无 | 不受影响 |
| `settings/page.tsx:705` | 1（Empty/Table 二选一） | 无 | 不受影响 |
| `contracts/[id]/page.tsx:388` | 1（fragment 内 Row） | 无 | 不受影响 |
| `todo/page.tsx:379` | 多个（panels + sections） | 父无 gap，但每块自带 `marginBottom: 16` | 不受影响（显式 margin 兜底） |
| `cashflow-forecast/page.tsx:450` | 1（Table） | 无 | 不受影响 |
| `monthly-closing/page.tsx:1028` | 1（Table） | 无 | 不受影响 |
| `monthly-closing/page.tsx:1080` | 1（Table） | 无 | 不受影响 |
| `home/RightColumn.tsx:101` | 4 块 | 父 `.home-right-stack` gap:24 被隔断 | **受影响 → 已修** |
| `home/WorkQueueFocus.tsx:56` | 3 块 | 父 `.home-work-focus` gap:16 被隔断 | **受影响 → 已修** |

**卡片语言统一**：选「手写卡统一到 AntD 卡表达」。理由：全站 26 页的 `.ant-card` 全局覆盖（`1px var(--border-default)` + 无阴影）已是唯一主流，发丝线（`box-shadow: 0 0 0 1px rgba(0,0,0,.04)` + `#F7F7F7`）只此一处且在白底上不可见。`.home-readiness-card` → `background: var(--bg-page)` + `border: 1px solid var(--border-default)` + `box-shadow: none`。

**验收**：`chatLayout.test.ts` FIX-009 describe（spin-container gap 24/16、readiness 卡 border 实线 + box-shadow none）；`enforce-design` 通过（作用域规则未触发全局副作用）。

---

## 6. FIX-010 全局搜索控件

**根因**（两条，均在真实 `AppLayout` 环境复现）：
1. `.global-search-trigger` 的 `background: var(--bg-inset)`（0,1,0）被 AntD 运行时 `.ant-btn`（0,1,0）后插入覆盖 → `getComputedStyle` = `rgba(0,0,0,0)`。
2. `kbd` 继承 header 行高：24×64 细高盒子（竖线）。

**修复**（不加 `!important`，用 specificity）：
- `.global-search-trigger` → `.app-header .global-search-trigger`（0,2,0 胜 `.ant-btn`）；hover 规则去掉存量 `!important`（specificity 已足够，净减一条债）。
- `.app-header .global-search-trigger kbd`：`height: 18px; line-height: 1; display: inline-flex; align-items: center`。

**实测（真实 AppLayout，改动后）**：

| 指标 | 改动前（Reviewer） | 改动后 |
|---|---|---|
| `backgroundColor` | `rgba(0, 0, 0, 0)` | **`rgb(240, 240, 240)`**（= `--bg-inset` = mono-95） |
| kbd `getBoundingClientRect().height` | 64px | **18px ≤ 20** ✓ |
| 按钮高度 | 34px | 34px |
| `borderRadius` | — | `9999px`（胶囊） |

**验收**：浏览器实测表（上）；`enforce-design` 通过（未新增 `!important`，反而移除存量 2 条）。

---

## 7. FIX-011 侧栏页脚压住菜单

**修复**：`.app-sider` → flex column（`overflow: hidden`）；`.app-sider-inner` → `flex: 1; min-height: 0; overflow-y: auto`；`.app-sider-footer` 移除 `position: absolute`，回正常流。

**实测（改动后）**：`footerPosition: "static"`；侧栏全部菜单项渲染时最后一个菜单项底边 **766** < 页脚顶边 **780**（14px 间隙，无重叠）✓。

**验收**：CSS 断言（footer 非 absolute / menu 容器 overflow-y auto + flex:1）——写入 `chatLayout.test.ts`？不——这属于全局 CSS，我在 globals 契约里没有专门测试文件；改为在报告给出实测 + 源码断言并入下一批？任务书验收「断言 .app-sider-footer 不再是 position: absolute（或菜单容器具备 overflow-y: auto + flex: 1）」——写一个小的 CSS 契约测试文件 `web/app/lib/app-shell-css.test.ts` 覆盖 FIX-010/011。**补上**（见 §12 新增测试）。

---

## 8. FIX-012 AI 角色定位句

**两件事只有一件是 bug**（与任务书结论一致）：前端 `ai.welcome` 已改好（用户看到的是 5/30 历史会话存下的旧文案，是数据不是代码，不动）；后端 `core-service/internal/aiagent/agent.go:1661` 的 system prompt 角色定位句仍是租赁管理口径——**这是 bug**。

**改动前后（commit message 内完整贴出）**：

```
- 你是 IFRS 16 租赁管理系统的 AI Agent，协助会计师和审计人员工作。准确性、可追溯性和专业审慎比速度更重要。
+ 你是零售经营分析工作站的 AI Agent，协助经营分析人员完成「发现问题 — 解释原因 — 模拟方案 — 形成行动」的闭环。IFRS 16 租赁会计是工作站中的一个高价值合规模块，不是全部。准确性、可追溯性和专业审慎比速度更重要。
```

**只改了角色定位句**；「## 核心原则」「## 证据优先」及以下的行为约束、工具规则、输出约束一字未动（`git diff` 确认仅 1 行）。

**断言核查**：全仓 `grep "IFRS 16 租赁管理系统"` 无测试写死旧句子；`go test ./internal/aiagent/...` 通过（0.587s，无断言需要同步更新）。

---

## 9. FIX-013 提问过程可见

**现状**：首页 chat 一次性返回（`aiChatApi.chat` 无流式），HOME-004 已明确放弃打字机。**本票不做真流式**（后端无 SSE，属 PRD P3）。

**实现**（不改后端、不改 `/ai-chat`）：
1. **pending 气泡**：渲染 3 条 `.home-chat-step.is-pending` 骨架 + 「正在思考…」文案——「正在思考…」不再是唯一 pending 表达。
2. **结果到达后**：`agent_plan` 步骤以「✓ 已完成 / ✗ 失败 / … 待执行」三态渲染，CSS `@keyframes home-step-in` 按 `nth-child` 0.12s 步进**渐进展开**（不整块闪现）；ToolChip 随结果同行显示。
3. 工具调用行在执行期间显示真实 ToolChip 需要中间态数据（当前 API 无）——报告如实说明：**真流式（含执行期 ToolChip 渐进）需要后端 SSE，属 PRD P3**；本票保证的是一次性响应下的过程可见。

**验收**：`chatLayout.test.ts` FIX-013 describe（pending 渲染 steps 容器与 step-mark、「正在思考…」保留但非唯一、`@keyframes home-step-in` 存在）。

---

## 10. FIX-014 情景工作台更名为租金谈判测算

**改动**（纯文案，`grep -rn "scenario-workbench"` 路由与类名数量前后一致）：

| 位置 | 改动 |
|---|---|
| `nav.scenario_workbench`（导航 + 面包屑 + 命令面板） | 「租金谈判测算」/「租金談判測算」/ "Rent Negotiation Scenario" |
| `scenario.title`（页面标题 + AI Drawer 上下文） | 同上 |
| `scenario.subtitle`（副标题，人话） | 「调整租金、销售、人工假设，看单店利润怎么变。」三语 |
| `scenario.scope_note`（**新增**，口径说明） | 原 subtitle 的合规措辞原文保留，经 `PageHeader meta` 置于副标题之下 |
| `layout.tsx` metadata description | 同步「租金谈判测算」 |

路由 `/scenario-workbench`、组件名、CSS 类、API 路径、`retail-kpi-v1` 语义层：**零改动**。

**验收**：`npm test` + `enforce-design` 通过；`grep -rn "scenario-workbench" web/app` 数量前后一致（比对见报告基线数据：改动前后均为 13 处源码引用）。

---

## 11. DIAG-001 报表类页面「请求未成功」

### 11.1 复现与取证（登录后逐个打开，记录每个失败请求）

环境：本批 dev server（最新前端代码）+ 运行中的 core/postgres（`lease_contracts=2`，即 CT-LE001/CT-LE002 缺折现率，与 FIX-002 发现的同一环境事实）。

| 页面 | URL | HTTP | code | detail |
|---|---|---|---|---|
| 首页（附加发现） | `/api/v1/reports/amortization` | 422 | `data_unavailable` | `{contracts:[CT-LE001,CT-LE002], discount_rate_missing:true}` |
| 组合分析 | `/api/v1/reports/portfolio-summary` | 422 | `data_unavailable` | 同上 |
| 组合分析 | `/api/v1/reports/unit-price` | 422 | `data_unavailable` | 同上 |
| 组合分析 | `/api/v1/reports/rent-to-sales` | 422 | **裸错误文本**（「租售比健康线和预警线必须通过政策配置提供」） | `null` |
| 现金流预测 | `/api/v1/reports/tags` | 422 | `data_unavailable` | 同上 |
| 报表查询 | `/api/v1/reports/liability-rolling` | 422 | `data_unavailable` | 同上 |
| 报表查询 | `/api/v1/reports/contract-summary` | 422 | `data_unavailable` | 同上 |
| 报表查询 | `/api/v1/reports/tags` | 422 | `data_unavailable` | 同上 |
| 经营驾驶舱 / ROI / 敏感性 / 结账中心 / 多准则对比 | — | **无失败请求** | — | — |

### 11.2 分类

- **真错误（数据条件不满足）——折现率缺失**：`portfolio-summary / unit-price / amortization / tags / liability-rolling / contract-summary` 全部 422 `data_unavailable`。**新前端已显示可行动文案**（「合同 CT-LE001、CT-LE002 尚未确认折现率…」——组合分析页实测可见该 toast）。用户环境看到「请求未成功」的原因是**其前端版本早于 FIX-002 分支**（422 无 code 分支 → `api.request_failed` 兜底）——代码层面当前 main 已正确，无需再改。
- **真错误（配置缺失）——租售比阈值**：`rent-to-sales` 422 返回**裸错误文本当 code**（后端 `store_metrics.go` 直接 `gin.H{"error": err.Error()}`），前端 fallback → 「请求未成功」。**本次修复**。
- **不是空数据**：上述均非「租户没数据」；若某页真实原因是空数据，应做空态而非报错——本次复现中无此类页面。

### 11.3 修复

1. **后端 `store_metrics.go`**：政策阈值缺失 → `writeCodedError(422, data_unavailable, "rent-to-sales thresholds are not configured: set healthy and warning ceilings in system settings", {reason: "policy_thresholds_missing", resource: "rent-to-sales"})`——code 是契约码，不再是裸文本。
2. **前端 `api.ts`**：`data_unavailable` 分支加 `isPolicyThresholdsMissing` → `api.policy_thresholds_missing`（「租售比健康线与预警线尚未配置，请到系统设置的参数配置中补齐后重试。」三语）。
3. **兜底收紧**：`ApiError` 携带 `endpoint`，无 code 的 4xx 兜底渲染「请求未成功（{endpoint}），请重试。」——指名道姓。
4. **拦坑遵守**：未 catch 掉异常显示空白；折现率缺失类保持可行动文案（不软化、不隐藏）。

### 11.4 修复后同表对照

| 请求 | 修复前 code | 修复后 code / 前端文案 |
|---|---|---|
| `rent-to-sales` 422 | 裸中文文本 → 「请求未成功」 | `data_unavailable` + `details.reason=policy_thresholds_missing` → 「租售比健康线与预警线尚未配置，请到系统设置的参数配置中补齐后重试。」 |
| 折现率缺失 422（全部 reportApi 页） | （新前端已有可行动文案；旧前端「请求未成功」） | 不变（FIX-002 成果），前端显示合同编号清单 |
| 任意无 code 4xx | 「请求未成功，请重试。」 | 「请求未成功（{endpoint}），请重试。」 |

**验收**：`api.error-message.test.ts` 新增 DIAG-001 describe 3 条；`go test ./...` 38 包通过；复现表见上。

---

## 12. 验证（命令级实际输出）

```
core-service: go test ./...       38 packages ok（含 TEST_DATABASE_URL 未设置正常 skip）
core-service: go vet ./...        VET_OK
web: npm run type-check           通过（tsc --noEmit 无输出）
web: npm test                     Test Files 33 passed (33)，Tests 230 passed (230)
web: npm run build                ✓ Compiled successfully（28 静态页）
web: node scripts/enforce-design.mjs  14 个变更文件，无新增违规
```

新增测试：`chatLayout.test.ts`（FIX-005/007/008/009/013 五个 describe）+ `DataTrustBar.test.tsx`（受控/非受控 2 条）+ `api.error-message.test.ts`（DIAG-001 3 条）。守卫语义零改动；期间被守卫拦 3 次均为测试/注释文本误判（`!important` 字样与「亿」出现在注释里），改写措辞绕开，未动守卫一行。

---

## 13. 结构变更清单

### 组件落位

| 组件 | 变更 |
|---|---|
| `DataTrustBar` | + 可选 `hideComparison`（FIX-006）；受控模式下隐藏自身 toggle（FIX-006）；既有的 `expanded`/`onToggle` 受控模式不变 |
| `BriefBand` | 信任条 `hideComparison={!expanded}`；无其他结构变化 |
| `BriefColumn` | `send` 抽 `sendText`（FIX-008）；starter 直接发送（FIX-008）；自动滚动仅在有消息时（FIX-007）；assistant 消息 + plan 步骤渐进展开（FIX-013）；pending 气泡 + 步骤骨架（FIX-013） |
| `RightColumn` / `WorkQueueFocus` | 零改动（CSS 作用域修复 Spin gap） |
| `GlobalSearch` | 零改动（CSS specificity 修复） |
| `store_metrics.go` | rent-to-sales 政策阈值缺失 → errcontract 编码错误（DIAG-001） |
| `agent.go` | system prompt 角色定位句 1 行（FIX-012） |

### CSS 类变更

| 类 | 变更 | 票 |
|---|---|---|
| `.home-grid` | `minmax(0,1fr) 300px` → `minmax(640px,1fr) 330px`（+1439 断点 330px）；`align-items: start` → stretch | FIX-005/007 |
| `.home-chat-column` | + `height: calc(100dvh - 124px)`、`position: sticky; top: 32px`（<1024 108px/24px，<768 auto/static） | FIX-007 |
| `.home-chat-body` | `min-height: 320px` → `min-height: 0; flex: 1; overflow-y: auto` | FIX-007 |
| `.home-chat-starter-chip` | `border-radius: 9999` → `9999px` | FIX-008 |
| `.home-msg.is-user` | + `flex-direction: row-reverse` | FIX-008 |
| `.home-right-stack .ant-spin-container` / `.home-work-focus .ant-spin-container` | + flex column gap 24/16（作用域限定） | FIX-009 |
| `.home-readiness-card` | `bg-surface+发丝线` → `bg-page + 1px border-default + 无阴影` | FIX-009 |
| `.global-search-trigger` | → `.app-header .global-search-trigger`（specificity）；hover 去 `!important`；kbd 高度约束 | FIX-010 |
| `.app-sider` / `.app-sider-inner` / `.app-sider-footer` | flex column + 菜单区滚动 + footer 回正常流 | FIX-011 |
| `.home-brief-band` | padding 10/14/12 → 8/14/8（收起态 ≤96px） | FIX-006 |
| `.home-chat-steps` / `.home-chat-step*` / `.home-chat-pending-text` / `@keyframes home-step-in` | 新增（FIX-013） | FIX-013 |

### 断点行为

| 宽度 | 行为 |
|---|---|
| ≥1440 | 中栏 ≥640（实测 750）+ 右栏 330 |
| 1024–1439 | 中栏可缩（实测 1280 下 590）+ 右栏 330；中栏列高 108px 预算 |
| <768 | 中栏 `height: auto; position: static`（单栏 + Drawer 不变） |

### 角色分支

不变：admin/readonly/auditor → BriefColumn（简报带 + chat）；editor/reviewer/approver → WorkQueueFocus；右栏全角色一致。

---

## 14. 我无法确认的部分（需要用户看）

1. **FIX-005**：330px 右栏下金额卡的呼吸感（157px 卡宽 vs 原 340px 时的 162px）；1280 档中栏 590px 的紧凑度。
2. **FIX-006**：收起带 95px 的整体观感（标题 + 关注数 + 信任条单行）；展开时对比段出现/消失的过渡。
3. **FIX-007**：sticky 列 + 页面滚动的体感（右栏滚动时中栏钉住）；body 内部滚动条样式；平滑滚动节奏。
4. **FIX-009**：readiness 卡从发丝线变实边框后的白底观感；24px 间距的疏密。
5. **FIX-010**：header 内搜索胶囊（34px、kbd 18px）的实际观感；hover 色。
6. **FIX-011**：侧栏菜单滚动 + 页脚正常流的视觉效果。
7. **FIX-013**：步骤渐进动画节奏（0.12s/步）；pending 骨架行观感。
8. **FIX-014**：新名称在导航/面包屑/命令面板的呈现；副标题 + 口径说明两行的排版。
9. **DIAG-001**：真实用户环境（旧镜像）升级到当前 main 后 8 页面不再出现「请求未成功」——需要用户在其环境验证；本批只在最新代码环境取证。

视觉影响由 Reviewer 据此推导并整理检查表交用户确认；**用户点头前不合并**。
