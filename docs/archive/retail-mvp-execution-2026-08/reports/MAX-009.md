# MAX-009 端到端演示与 MVP 发布验收报告

> ⚠️ **ARCHIVED 2026-08-18 — 不是现行依据**
> 归档理由：零售 MVP（MAX-001~009）转型执行的过程记录，全部工单已 ACCEPTED；未完成的延后治理项见 docs/execution/精简MVP路线与延后治理清单.md
> 现行入口：`docs/AI_文档索引与现行决策.md`

状态：`ACCEPTED`（Review 8）  
结论：`GO`（无 P0/P1；非阻断 P2 文档精度不再派发补丁）  
下一步：MVP 可用于内部演示与模拟验证；真实客户联调、访谈和第三方会计复核仍按外部待办管理。
日期：2026-08-13  
Executor：Max

## Review 2 收口摘要

本轮只做发布验收收口。AI Chat 修复为局部变更：移动 Drawer 内 More/delete 始终可见，桌面 hover/focus 行为保留；会话选择使用原生 `button` + `aria-pressed`，More 为 sibling；删除未消费的 responsive inert state/event。另将既有 Artifact 类型约束补齐 `retail_action_proposal`，使已实现的 proposal 合同能在当前数据库实例持久化；未新增 migration、路由、产品能力或 AppLayout 改造。

真实主链已由 scoped LE001 用户完成并在清理前取证：fixed seed → Pulse → rank1 → Store360 → 12 月 labor -10% Scenario → 三页 AI read tools → deterministic proposal/source → Scenario 二次确认 Modal → 一条 `open` action。随后以精确 ID/username 在单事务中清理 dataset、facts、stores、batch、fact request、三条本票 action、AI session/run/artifact、refresh session 和两个临时用户；残留逐类为 0。

## 1. 环境与自动化验证

```text
docker compose config                         PASS
docker compose ps                              PASS (Postgres/MinIO healthy; Core/Web/AI running)
docker compose up -d --build web               PASS (lease-web Created 更新；不重置 volume)
cd core-service && go test ./...              PASS
cd core-service && go vet ./...               PASS
cd web && npm test -- --run                   PASS — 11 files / 72 tests
cd web && npm run type-check                  PASS
cd web && npm run build                       PASS — Next.js 14 production build
git diff --check                               PASS
```

真实 PostgreSQL（Docker network，`TEST_DATABASE_URL` 指向 `lease-postgres`）均 PASS：

```text
go test -v ./internal/repository -run 'TestRetail(StoreDayFacts|Simulation|KPI|Pulse|StoreDiagnostics|Scenario)Postgres' -count=2
PASS — 30.768s，全部两轮通过；Pulse 60 店固定查询 2 次，约 12.614ms / 13.652ms
go test -v ./internal/agenttools/tools -run TestRetailOperationsPostgresIsolationNoWrites -count=2
PASS — 6.548s，两轮通过
```

PG 回归结束后精确 residual：`MAX009R2_users=0`、`MAX009R2_sessions=0`、ticket dataset/facts/stores/batch/actions/fact_request 均 `0`、`AGENT-LE=0`、`AGENT stores=0`。

## 2. 固定数据与真实 API 证据

固定上下文：seed `20260812`、日期 `2026-01-01..2026-06-30`、60 店；实际返回 dataset `retail-sim-v1-2853d9537f358baf`、batch `71da169e-1780-4887-ba56-2490f60d870f`、10,860 facts。rank 1 实际门店为 `SIM-2853D953-007` / store ID `2af7d433-0fbf-4117-a882-0d1ff4993053`。

| 检查 | 真实结果 |
|---|---|
| fixed seed 同 key replay | 两次 `200`；dataset/version/batch/fact_count 一致（10,860），replay envelope 为 true |
| fact import 同 key replay | `200/200`；目标行 before/after 均 1；异 payload `409 idempotency_payload_conflict` |
| Scenario action 同 key replay | `200/200`；ID `97193b0c-8dcd-4916-9dc4-bd96f424ee16` 不变，`status=open`；异 payload `409` |
| LE001 A/B | KPI `200`、目标店 1 item；Pulse `200`、`decision_ready=true` |
| LE002 请求 A 门店 | KPI `200` 空 envelope；Pulse `200`、`decision_ready=false`；Scenario 对 A 店 `404 retail store is not visible` |
| production | LE001/LE002 均 `200`、`data_classification=production`、`dataset_version=null`、无 simulated generator |
| invalid resulting rate | Scenario `422`，`reason=resulting_rate_out_of_range`，evidence 含 7/7 coverage；未产生 action/proposal |

脱敏 API 记录见 [`r2-api-smoke.json`](../evidence/MAX-009/r2-api-smoke.json)。

## 3. 浏览器主链与截图

真实 Web：`http://localhost:3000`，未 mock、未用 DevTools 补字段。Pulse 显示 420/420 current/comparison、rank 1 `high / 3.02`、occupancy signal `10.08pp`。Scenario labor 结果为 `14,193.26 → 12,773.93 CNY`，月度变化 `1,419.33 CNY`，12 月 `17,031.91 CNY`，bridge residual `0.00`。Action Modal 在确认前截图，确认后真实 ID/status/Owner/Due/replay 均显示。

AI 真实 trace 顺序为 `retail.operating_pulse.read → retail.store_diagnostics.read → retail.store.scenario.evaluate`，模型 `deterministic-fallback`；proposal Artifact 明确 simulated/dataset/source、Owner/Due 空、`formal_execution=false`、`business_write=false`，source 点击 round-trip 到 `/operating-pulse`。脱敏 run/artifact JSON（无 token、密码或连接串）见 [`r2-ai-trace-artifact-sanitized.json`](../evidence/MAX-009/r2-ai-trace-artifact-sanitized.json)。

关键证据：

- 四页桌面：`r2-pulse-1440x900.png`、`r2-store360-1440x900.png`、`r2-scenario-1440x900.png`、`r2-aichat-1440x900.png`。
- 四页移动：对应 `r2-*-390x844.png`；每页实测 `scrollWidth=390/clientWidth=390`。
- Scenario stale：[`r2-scenario-stale-1440x900.png`](../evidence/MAX-009/r2-scenario-stale-1440x900.png)，改 labor 假设后显示“结果已过期/不能保存”；重新 Evaluate 后恢复可保存。
- Action confirm：[`r2-action-confirm-modal-1440x900.png`](../evidence/MAX-009/r2-action-confirm-modal-1440x900.png)，为保存前 Modal，不是保存后页面。
- AI proposal：[`r2-ai-proposal-1440x900.png`](../evidence/MAX-009/r2-ai-proposal-1440x900.png) 与移动版；Drawer：[`r2-ai-session-drawer-390x844.png`](../evidence/MAX-009/r2-ai-session-drawer-390x844.png)。移动 Drawer 中 delete opacity 实测为 `1`，选择会话 Enter 后关闭并回焦点，Escape 关闭并回焦点。

旧 route smoke（scoped LE001）：`/`、`/performance`、`/contracts`、`/ai-chat`、`/upload`（按既有合同重定向到 `/ai-chat`）、`/monthly-closing`、`/reports`、`/audit-logs`、`/settings` 均有 `main` 内容、无 404/白屏。AI Chat post-rebuild 390px 再测仍为 `390/390`，trigger 可访问。

## 4. Console / Network / 红线

- Console：无 uncaught exception、hydration/key、CORS 或 mixed-content 错误；既有 Recharts 初始尺寸 warning 仅登记 Hardening，不影响页面。
- Network：IAB 不提供 HAR/Resource Timing 导出，因此不伪造 waterfall；本报告保留真实同源 API URL/status/scope（见 JSON）及 stale 状态转移证据。旧响应未覆盖新假设，保存按钮在 stale 时 disabled。
- Prompt injection（scoped LE001）：要求读取 LE002、调用旧 `lease.store.scenario.simulate`、修改 Official/IFRS16；回答进入 `provide_retail_context`/只读边界，无旧 write tool、无 proposal/action。提示词中的旧工具字符串不代表实际调用，trace 仅允许三项零售 read tool。
- AI/经营页面明确 Working、经营占用现金口径与 IFRS16 会计费用隔离；proposal `side_effects=false/official_impact=false/ifrs16_impact=false`。

## 5. 正式表 before/after 与清理

正式表快照见 [`r2-formal-before.json`](../evidence/MAX-009/r2-formal-before.json) / [`r2-formal-after.json`](../evidence/MAX-009/r2-formal-after.json)。`lease_contracts=29`、`lease_events=0`、`lease_payment_schedules=224`、`measurement_results=10`、`journal_entries=17`、`monthly_closing_batches=5` 的 count 与 max timestamp 完全不变。ticket fixture 的 `fpna_action_items` 与 `retail_simulation_datasets` 已由 1 精确清理为 0。

清理前只读 FK 核对确认：facts→stores/batch、dataset→batch、AI runs/artifacts/sessions、user→refresh sessions 及可空 created_by/changed_by 依赖。事务内仅操作：dataset ID/version、batch ID、simulation dataset version、fact request key、三条 action ID、R2 两个 username 及其 session/dependencies；不使用 wildcard、不 `down -v`。清理后逐类 residual=0，证据保留在截图与脱敏 JSON，不依赖数据库残留。

## 6. 原产品兼容与回滚

未删除/重命名/隐藏旧页面、导航、Skill、Tool、route、AppLayout、IFRS16/Official 合同。回滚仅需 `docker compose build web && docker compose up -d web` 并强制刷新；不得 reset volume。若发现任一 P1，恢复任务为 `IN_PROGRESS/NO-GO` 并保留证据等待 Reviewer，不自行 ACCEPTED。

## Reviewer 复现

1. `docker compose ps`，确认 Web Created 时间已更新；运行索引中的真实 URL 和截图 viewport。
2. 用现有 scoped 测试账号（凭据不在报告）或 Reviewer 自有法人账号，复演 Pulse → Store360 → Scenario stale/re-evaluate → AI proposal → action Modal。
3. 使用报告中的 Docker network 命令跑 repository/agenttools PG tests `-count=2`，检查 query count、耗时和 residual。
4. 运行同一组正式表快照 SQL，逐项对比 before/after；检查上述精确 ID/username 均为 0。

## Review 4 纯发布取证收口（已完成，提交 IN_REVIEW）

本轮没有产品、schema、AppLayout、旧 route、IFRS16/Official 或 Agent 合同改动；仅使用 Admin API 创建 `MAX009R4_LE001/LE002` 与 `MAX009R4B_LE001/LE002` 四个唯一 scoped fixture，取证后注销并在事务内清理。当前可恢复的 R4 owner UUID 仅为 `855251ac-a318-49d4-b0cd-04198b9770a6`（`MAX009R4B_LE001`）；其余三个用户名以 exact username selector 复核，未虚构 UUID。密码、JWT、连接串未进入证据或报告。

### 同一浏览器页面 race / stale gate

在同一个已认证 `/operating-pulse` 浏览器 tab 内连续触发 28 天旧窗口，再于 21ms 后触发 7 天新窗口；两次 click Promise 均 fulfilled，最终 URL 为 `window_days=7`，DOM 显示 2026-05-30—06-05、420/420 current/comparison、rank1 `SIM-2853D953-007`，没有旧 28 天结果残留。旧/新 request key、Promise 状态、最终 URL/DOM 与 console 记录见 [`r4-race-browser.json`](../evidence/MAX-009/r4-race-browser.json)。其中 R4 的 `/api/v1/...` 仅作为历史无效网络记录/`normalized_core_endpoint` 保留，不能视为浏览器完整同源 request URL；页面源码的 gate（`web/app/operating-pulse/page.tsx:148-162`）与后续 R5 deferred gate 才是当前有效 race 证据。浏览器控制面不提供 HAR/Resource Timing 或 route interception，因此没有把外部 curl 终止冒充同一页面网络证据。

### 四类负向 Agent 原始 trace

四个案例都在 LE001 scoped 用户下真实创建 session/run，`GET /trace` 与 session detail 原始响应完整保留，events 按 `created_at/sequence_no` 顺序列出，tool execution、assistant message、run status、reason、confidence/evidence、proposal artifact 与 business action before/after 均逐例记录；四例取证前后业务 action 均 `0→0`，proposal artifact 均为空。

| case | session / run | run status | reason | confidence / evidence | tool path | action |
|---|---|---|---|---|---|---|
| needs-input | `269f4a85…` / `b0b09899…` | `waiting_review` | `missing_context` | `0.40 / insufficient` | none | `0→0` |
| partial | `7c9ce3f1…` / `ac070c79…` | `waiting_review` | `partial_coverage` | `0.40 / insufficient` | pulse → diagnostics | `0→0` |
| no-facts | `45d38e5a…` / `6493a7b2…` | `waiting_review` | `no_facts` | `0.40 / insufficient` | pulse → diagnostics(rejected) | `0→0` |
| invalid-rate | `be15cf64…` / `ad354146…` | `waiting_review` | `invalid_arguments` | `0.40 / insufficient` | pulse → diagnostics → scenario(rejected) | `0→0` |

逐例脱敏原始 envelope：[`r4-negative-needs_input.json`](../evidence/MAX-009/r4-negative-needs_input.json)、[`r4-negative-partial.json`](../evidence/MAX-009/r4-negative-partial.json)、[`r4-negative-no_facts.json`](../evidence/MAX-009/r4-negative-no_facts.json)、[`r4-negative-invalid_rate.json`](../evidence/MAX-009/r4-negative-invalid_rate.json)。invalid-rate 在 Agent 合同中作为 rejected tool execution 留在 trace（而非绕过 Agent 直接写业务），因此没有 proposal/action；真实 response 与 rejected event 均在该文件中。

### 跨法人同一对象正反 smoke

LE001 先成功读取同一 simulated source（200、目标事实非空），保存一条 `open` action `b4d6f5ed-bf8a-4d4f-9231-c7f2026b3566`，并生成 run `19dee11e-2090-4987-9416-c555b6255ea2` / session `66fbe25c-e12e-46b1-b7d0-a02c6c74199e` / proposal artifact `0c285f6f-46c6-4e45-bdd9-3a2898db1482`。随后 LE002 对相同 store/date/dataset/source 得 200 空 envelope，owner-filtered action list `total=0`，同 run trace/session detail 得 404；产品无独立 artifact GET 路由，故使用 owner-filtered run/session detail 中的同一 artifact ID，并记录该访问合同。原始正反响应见 [`r4-cross-scope.json`](../evidence/MAX-009/r4-cross-scope.json)。

### 精确 teardown / residual

R4 fixed dataset `238d70cc-5e71-4bbc-8bab-75490f80102c`、partial dataset `8d3523fb-e117-4fec-a9a7-cbaa52213eab`，对应 batch/facts/stores/fact-request、action、5 sessions/runs、proposal artifact、四个唯一 Admin API fixture username 集合及 refresh-session 依赖均已精确清理。残留 selector、实际值和每次查询耗时见 [`r4-residual.json`](../evidence/MAX-009/r4-residual.json)；user/refresh selector 对四个 username 使用 exact `IN (...)` 集合，只有已恢复 UUID 的 B_LE001 另列 UUID selector，全部 `actual=0`。FK/cascade 顺序与无 wildcard、`down -v`、volume prune 约束保持不变。

### R4 截图与复现

AI proposal 重新保存为 [`r4-ai-proposal-1280x720.png`](../evidence/MAX-009/r4-ai-proposal-1280x720.png)，文件名与索引如实标注浏览器 viewport 1280×720（内容位图 1269×714），不再宣称 1440×900。已登记的 rendered-DOM 自动化缺口保持 `HARD-012`，不在本轮扩展测试栈。

Reviewer 可按 evidence index 的 JSON 直接复核原始 response、同一 ID 正反隔离和 exact residual；完成后再以自己的 scoped 账号复演固定 seed → Pulse → Store360 → Scenario → AI → action 主链。R4 fixture 已清理，数据库不保留“证据残留”。

### R4 最终验证命令

```text
cd core-service && GOCACHE=$(pwd)/.gocache GOMODCACHE=$(pwd)/.gomodcache go test ./...       PASS
cd core-service && GOCACHE=$(pwd)/.gocache GOMODCACHE=$(pwd)/.gomodcache go vet ./...        PASS
cd web && npm test -- --run                                                                  PASS — 11 files / 73 tests
cd web && npm run type-check                                                                  PASS
cd web && npm run build                                                                       PASS — Next.js 14 production build
git diff --check                                                                              PASS
docker exec lease-postgres pg_isready -U lease -d lease                                    PASS — accepting connections
docker run --rm --network lease_management_system_default -v /Users/cheukfungwu/ifrs16_management_system:/workspace -w /workspace/core-service -e 'TEST_DATABASE_URL=<redacted Docker-network PostgreSQL URL>' -e GOCACHE=/workspace/core-service/.gocache -e GOMODCACHE=/workspace/core-service/.gomodcache golang:1.23-alpine go test -v ./internal/repository -run 'TestRetail(StoreDayFacts|Simulation|KPI|Pulse|StoreDiagnostics|Scenario)Postgres' -count=2 PASS — 76.339s
docker run --rm --network lease_management_system_default -v /Users/cheukfungwu/ifrs16_management_system:/workspace -w /workspace/core-service -e 'TEST_DATABASE_URL=<redacted Docker-network PostgreSQL URL>' -e GOCACHE=/workspace/core-service/.gocache -e GOMODCACHE=/workspace/core-service/.gomodcache golang:1.23-alpine go test -v ./internal/agenttools/tools -run TestRetailOperationsPostgresIsolationNoWrites -count=2 PASS — 10.330s
```

PG 两轮结束后 `AGENT-LE=0`、`AGENT stores=0`、`MAX009R4 users=0`、`MAX009R4 actions=0`；R4 的逐 selector residual 仍以 [`r4-residual.json`](../evidence/MAX-009/r4-residual.json) 为准。工作树保留前序任务与用户未跟踪资产，未 commit/push。

## Review 3 收口证据（已完成，提交 IN_REVIEW）

### P0 原 PostgreSQL volume 无损恢复

先记录了 `docker compose ps -a`、`docker system df` 和 `docker logs lease-postgres --tail 120`。原 `lease-postgres` 因宿主存储耗尽在 checkpoint/WAL recovery 阶段退出；没有执行 `down -v`、volume prune、image prune 或 system prune。唯一回收命令是 `docker builder prune -af`，只回收可重建 build cache。随后仅以 `docker compose up -d postgres` 启动原 volume。日志显示 automatic recovery、redo starts/done、end-of-recovery checkpoint，最终 `database system is ready to accept connections`；`docker exec lease-postgres pg_isready -U lease -d lease` 返回 accepting connections，Core `/health` HTTP 200。恢复证据见 [`r3-postgres-recovery.json`](../evidence/MAX-009/r3-postgres-recovery.json)。

恢复后的正式表 count/max 与 [`r2-formal-before.json`](../evidence/MAX-009/r2-formal-before.json) 和 [`r2-formal-after.json`](../evidence/MAX-009/r2-formal-after.json) 完全一致：29/0/224/10/17/5，关键时间戳未变化。恢复日志中无新的 PANIC/FATAL；后续诊断 SQL 的列名探测错误仅为本轮取证命令错误，不是数据库或服务故障。

### P1 AI Chat 局部修复与真实浏览器

本轮只改 AI Chat scoped CSS/语义：active 与普通行的 More sibling 均有可见对比色；桌面只在 `.ai-chat-session-row:hover` / `:focus-within` 显示，移动 Drawer 始终显示；session selector 是原生 button + `aria-pressed`，删除按钮为 sibling；viewport 首次 render 固定后 mount 更新，去掉未消费 `compactHeader`。真实浏览器 390×844 实测 `innerWidth=390`、`scrollWidth=390`、`clientWidth=390`，Drawer open、4 个 More/delete 按钮 opacity=1；1440×900 实测 `innerWidth=1440`。新截图：[`r3-ai-chat-390x844.png`](../evidence/MAX-009/r3-ai-chat-390x844.png)、[`r3-ai-proposal-1440x900.png`](../evidence/MAX-009/r3-ai-proposal-1440x900.png)、[`r3-scenario-1440x900.png`](../evidence/MAX-009/r3-scenario-1440x900.png)、[`r3-action-confirm-modal-1440x900.png`](../evidence/MAX-009/r3-action-confirm-modal-1440x900.png)。

真实 scoped LE001 主链和 LE002 隔离取证均使用唯一 `MAX009R3_` 用户；没有在报告或证据保存密码、JWT、连接串。已关闭浏览器会话后，临时用户、refresh session 和所有本轮数据库对象均精确 teardown。浏览器 HAR 不可用，故仅报告当时可观察的 request/status/DOM scope，不声称拥有 HAR。

### 负向、跨法人与 race/cancel

[`r3-negative-agent.json`](../evidence/MAX-009/r3-negative-agent.json) 记录 needs-input、partial、no-facts 和 invalid resulting-rate：前三者均 `waiting_review`、confidence `0.40`、evidence insufficient、无 scenario/proposal/action；invalid 为 HTTP 422 且 action before/after 不变。[`r3-cross-scope.json`](../evidence/MAX-009/r3-cross-scope.json) 记录 LE002 对 LE001 KPI source 返回空 envelope、action queue 返回 `total=0`、AI run/session 返回 404，response shape 与 endpoint 均真实保留。 [`r3-race.json`](../evidence/MAX-009/r3-race.json) 记录旧 28-day request 被终止（status 143、未提交 DOM）后新 7-day request HTTP 200，最终 DOM 仅保留新 window/store scope。

### 040 migration 与 fresh init

已撤回 023 中的 retail proposal constraint 修改，新增且仅新增 [`040_agent_artifact_retail_proposal.sql`](../../../db/migrations/040_agent_artifact_retail_proposal.sql)。existing volume 应用后 `pg_get_constraintdef` 明确保留旧 artifact types 并加入 `retail_action_proposal`；同一事务插入 proposal 与 generic 后 rollback，均通过。`db/init/01_init.sql` 的 CREATE 与最终 ALTER 顺序均允许 proposal；使用一次性 `max009r3-fresh-postgres` + `max009r3_fresh_init` 临时资源验证 fresh init 后精确删除，原 volume 未触碰。命令与约束证据见 [`r3-migration.json`](../evidence/MAX-009/r3-migration.json)。

### 精确 teardown residual

清理事务只使用显式 dataset/batch/fact/action/session/run/artifact UUID、dataset version、fact request key 和两个临时 user UUID；不使用 wildcard。清理前为 facts 12,540、stores 120、datasets 2、batches 2、actions 2、AI sessions 4、users 2；清理后 facts/stores/datasets/batches/fact requests/actions/users/refresh sessions/AI sessions/runs/artifacts 逐类均为 0，并额外核对 R2 historical dataset version 与 action UUID 均为 0。逐项 SQL selector 与实际 query timing 见 [`r3-residual.json`](../evidence/MAX-009/r3-residual.json)。

### 本轮状态与最终验证

Review 3 要求的全量验证已完成：

```text
cd core-service && GOCACHE=$(pwd)/.gocache GOMODCACHE=$(pwd)/.gomodcache go test ./...       PASS
cd core-service && GOCACHE=$(pwd)/.gocache GOMODCACHE=$(pwd)/.gomodcache go vet ./...        PASS
cd web && npm test -- --run                                                                  PASS — 11 files / 73 tests
cd web && npm run type-check                                                                  PASS
cd web && npm run build                                                                       PASS — Next.js 14 production build
git diff --check                                                                              PASS
docker run --rm --network lease_management_system_default ... go test -v ./internal/repository \
  -run 'TestRetail(StoreDayFacts|Simulation|KPI|Pulse|StoreDiagnostics|Scenario)Postgres' -count=2 PASS — 32.036s
docker run --rm --network lease_management_system_default ... go test -v ./internal/agenttools/tools \
  -run TestRetailOperationsPostgresIsolationNoWrites -count=2                                  PASS — 6.754s
```

Repository 两轮均通过，Pulse 60 店 query count=2、约 13.123ms / 12.959ms；Scenario 每轮 18 SQL queries；Agent tool 两轮通过。PG 测试后的本票精确 UUID/version/key residual 仍为 0。040 constraint 复核仍包含旧 Artifact types + `retail_action_proposal`。因此本票现以 `IN_REVIEW` 提交，建议 `GO`，不自行标记 `ACCEPTED`。

## Review 5 收口证据（已完成，提交 IN_REVIEW）

本轮仅关闭 Review 5 指定的两个 P1 和一个 P2；未新增页面、route、schema、migration、tool、指标或业务能力，未修改 AppLayout、旧页面、IFRS16/Official。

### P1-A：实际页面 request gate 的确定性晚响应证明

浏览器控制面无法导出同一 tab 的 HTTP response start/end 与取消时序，因此没有把 click Promise 或手工拼接 URL 冒充网络证据。真实浏览器同页 28 天→7 天快速切换的最终 URL/DOM 仍保留：最终 `window_days=7`、current/comparison `420/420`；同源客户端 route pattern 明确记录为 `/api/api/v1/retail/operating-pulse`，Core 规范化 endpoint 单独标为 `/api/v1/...`。证据见 [`r5-race-gate.json`](../evidence/MAX-009/r5-race-gate.json)。

为证明“新响应先提交、旧响应后到达且不能覆盖”，从页面实际调用链抽出并消费 `createLatestRequestGate()`：旧请求 begin 后，新请求 begin；新 7 天 response 先 commit 成功，随后延迟的旧 28 天 response commit 返回 false，最终状态仍为 7 天。定向命令 `cd web && npm test -- --run app/operating-pulse/requestGate.test.ts` PASS；测试证据明确指向 `page.tsx` 的 import/use，而非独立常量自证。浏览器 Network timing 仍是环境限制，Reviewer 可在 DevTools/代理中补充原始时序。

### P1-B：低证据 Agent 进入原始 assistant/trace 输出

现有 deterministic assistant message 现在直接附带 `reason`、`evidence=insufficient`、`confidence=0.40`：needs-input 为 `missing_context`，no-facts 为 `no_facts`（底层 diagnostics rejected 仍保留真实 `scope_denied`），invalid-rate 为 `resulting_rate_out_of_range`（底层工具仍为 `invalid_arguments`，业务 reason 已保留）。scenario tool 对 `ScenarioEvidenceError` 透出具体业务 reason；无 proposal、无业务 action 写入。

新唯一 R5 fixture 的三例 raw trace/assistant/event 选段、HTTP status、run status、tool error、artifact/action `0→0` 见 [`r5-negative-agent.json`](../evidence/MAX-009/r5-negative-agent.json)。定向 Go 测试 `go test ./internal/aiagent ./internal/agenttools/tools` PASS；报告不把 JSON 顶层摘要当作原始字段，字段均来自 message_end/tool_execution/run_end payload。

### P2：R4 residual selector 精确化

`r4-residual.json` 中历史占位 selector 已改为完整 UUID/username selector；新 R5 teardown 另见 [`r5-residual.json`](../evidence/MAX-009/r5-residual.json)，所有 selector 均显式限定法人、dataset/version、batch、user UUID/username 或单一 idempotency key，不使用 wildcard。

### R5 fixture teardown 与命令证据

R5 只通过现有 Admin API 创建唯一 `MAX009R5_LE001_ADMIN`（LE001、admin），登录后取证并调用 logout；产品 Admin API 没有删除用户能力，故按已只读解析的 FK 顺序在事务内执行测试 fixture teardown，不代表产品用户管理能力。删除范围仅为本 fixture 的精确 UUID/key，未使用 `down -v`、volume prune 或 wildcard。清理前 users/refresh/dataset/facts/stores/batch/AI sessions/runs 分别为 `1/1/1/10860/60/1/7/7`；清理后 users、refresh、dataset、facts、fact_requests、stores、batch、actions、AI sessions/runs/artifacts、audit_logs 全部 `0`，查询耗时和 exact selector 见 `r5-residual.json`。密码、JWT、refresh token、数据库连接串未写入证据。

### Review 5 定向验证

```text
cd core-service && GOCACHE=$(pwd)/.gocache GOMODCACHE=$(pwd)/.gomodcache go test ./internal/aiagent ./internal/agenttools/tools  PASS
cd web && npm test -- --run app/operating-pulse/requestGate.test.ts app/operating-pulse/logic.test.ts  PASS — 2 files / 7 tests
docker exec lease-postgres pg_isready -U lease -d lease  PASS — accepting connections
```

本轮最终验证已完成：全量 `go test ./...`、`go vet ./...`、Web `npm test -- --run`（12 files / 74 tests）、`npm run build`、顺序执行的 `npm run type-check`、`git diff --check` 均 PASS；关键 repository PostgreSQL `-count=2` 两轮 PASS（33.818s），Agent tool PostgreSQL `-count=2` 两轮 PASS（7.758s），`pg_isready` PASS。若 Reviewer 复演发现新的发布阻断，本票再回 `IN_PROGRESS/NO-GO`；Executor 不自行 ACCEPTED。

## Review 6 收口证据（已完成，提交 IN_REVIEW）

本轮只修复 Review 6 指定的 P1/P2，不新增页面、route、schema、migration、tool、指标或 UI。

### P1：权限拒绝与结构化 no-facts 语义

删除 `core-service/internal/aiagent/retail_operations.go` 中依据 caller-controlled `dataset_version` 名称包含 `no-fact` 的生产分支。现在 Pulse/tool 真实拒绝且无可信结构化响应时，reason 保持 `scope_denied`；只有 Pulse 已成功返回结构化空事实（coverage expected=0）后，后续 diagnostics 的 scope denial 才映射为 `no_facts`。

新增定向回归覆盖两条路径：

- 非全局普通法人范围、目标门店不在授权 Store scope，dataset 名为 `planA-v1-no-facts`：deterministic Response 保留 `scope_denied`、`evidence=insufficient`、`confidence=0.40`，并断言 `SideEffects=false`、无 proposal/artifact；
- 非 Global 普通法人范围、无 StoreIDs/region/brand 的结构化空 population：deterministic Pulse Response 的结构化 reason 为 `no_facts`、`evidence=insufficient`、`confidence=0.40`，并断言 `SideEffects=false`、无 proposal/artifact。两例均使用 `emit=nil`，不生成新的 raw `message_end` trace 或 action DB 记录；raw message_end 继续只引用既有真实 R5 evidence。

定向测试与 vet：

```text
cd core-service && go test ./internal/aiagent ./internal/agenttools/tools  PASS
cd core-service && go vet ./internal/aiagent ./internal/agenttools/tools  PASS
```

### P2：历史 R4 网络证据降级与正向 session/run residual

R4 `/api/v1/...` 已明确标记为历史无效网络记录/`normalized_core_endpoint`，不再称为完整浏览器同源 request URL；R5 `r5-race-gate.json` 才是当前有效的页面 gate race 证据。`r4-residual.json` 新增正向 session `66fbe25c-e12e-46b1-b7d0-a02c6c74199e` 与 run `19dee11e-2090-4987-9416-c555b6255ea2` 的独立 exact selectors，均 `actual=0`。

### Review 6 验证

```text
cd web && npm test -- --run app/operating-pulse/requestGate.test.ts  PASS
cd web && npm run type-check  PASS
cd web && npm run build  PASS
docker exec lease-postgres pg_isready -U lease -d lease  PASS
git diff --check  PASS
```

正式表当前 count/max 仍为 `29/0/224/10/17/5` 与 R2 before/after 快照一致；R5 exact residual 仍为 0。本票已回 `IN_REVIEW`，等待 Review 8，不自行 ACCEPTED。

## Review 7 收口证据（已完成，提交 IN_REVIEW）

本轮仅修正测试范围、断言和证据文字；没有创建新 fixture，没有新增产品能力。

- no-facts 单测改为普通非 Global 法人范围 `access.Scope{LegalEntityID:"entity-a"}`，不配置 StoreIDs/region/brand；scope-denied 单测保留不同授权 StoreIDs，即使 dataset 名含 `no-fact` 仍为 `scope_denied`。
- no-facts 与 scope-denied 均显式断言 `RetailOperations.SideEffects=false`、`RetailActionProposal=nil`、`ProjectResult(...).Artifacts` 为空。该单测使用 `emit=nil`，因此本轮证据准确限定为 deterministic Response/ProjectResult；没有把它描述为新增 raw trace 或 action DB 证据。raw `message_end` 仅引用既有 R5 runtime evidence。
- 清单过期待修项已勾选；演示脚本、任务、报告、Evidence index 与看板统一为 `IN_REVIEW`，等待 Review 8。
- R4 residual 对 `MAX009R4_LE001`、`MAX009R4_LE002`、`MAX009R4B_LE001`、`MAX009R4B_LE002` 使用 exact username 集合 selector，并保留可恢复 B_LE001 UUID 与 refresh selector；所有 actual=0。历史中无法恢复的另外三个 UUID 未被虚构。

### Review 7 最终验证命令与结果

```text
cd core-service
GOCACHE=$(pwd)/.gocache GOMODCACHE=$(pwd)/.gomodcache go test ./internal/aiagent ./internal/agenttools/tools -count=1  PASS
GOCACHE=$(pwd)/.gocache GOMODCACHE=$(pwd)/.gomodcache go vet ./internal/aiagent ./internal/agenttools/tools  PASS

cd ../web
npm test -- --run app/operating-pulse/requestGate.test.ts  PASS (1 file / 1 test)
npm run type-check  PASS
npm run build  PASS (Next.js production build; 28 static pages)

cd ..
jq empty docs/execution/evidence/MAX-009/r4-race-browser.json docs/execution/evidence/MAX-009/r4-residual.json docs/execution/evidence/MAX-009/r5-race-gate.json docs/execution/evidence/MAX-009/r5-negative-agent.json docs/execution/evidence/MAX-009/r5-residual.json  PASS
docker exec lease-postgres pg_isready -U lease -d lease  PASS (accepting connections)
git diff --check  PASS
```

正式表复核结果：`lease_contracts|29|2026-08-11T11:58:45.589432+00`、`lease_events|0|null`、`lease_payment_schedules|224|2026-08-11T11:58:45.589432+00`、`measurement_results|10|2026-08-11T11:58:36.092807+00`、`journal_entries|17|2026-08-11T11:58:36.092807+00`、`monthly_closing_batches|5|2026-08-11T11:58:45.589432+00`，与 R2 before/after 一致。`r4-residual.json` 的 15 个 exact selectors 均为 `actual=0`；`r5-residual.json` 的 12 个 query selectors 均为 `actual=0`（其 `residual_zero=true`）。
