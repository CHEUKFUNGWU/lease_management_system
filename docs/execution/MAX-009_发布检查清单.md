# MAX-009 发布检查清单

状态：`ACCEPTED`；结论：`GO`（Review 8 无 P0/P1，非阻断增强不再返工）。

## 发布验收

- [x] PostgreSQL 原 volume 已从磁盘耗尽故障无损恢复，compose config/ps、pg_isready、Core/Web/AI 均 PASS；不重置 volume
- [x] fixed seed/version/source/store code、Pulse rank1 Golden 与 420/420 覆盖已真实复演
- [x] Pulse → Store360 → Scenario → AI → action 主链真实完成
- [x] Scenario stale/re-evaluate gate；保存前二次确认 Modal；确认后一条 open action
- [x] fixed seed replay、fact replay、异 payload 409、action replay ID 不变
- [x] scoped LE001/LE002 A/B API smoke：LE001 先证明同一 source/action/run/session/proposal artifact 存在，LE002 对相同目标得到空 envelope/404
- [x] AI 三 read tools、proposal Artifact、Owner/Due 空、formal_execution/business_write false、脱敏 trace JSON
- [x] needs-input/no-facts/invalid-rate：已删除 dataset 名称触发的魔法分支；真实 scope denial 保持 `scope_denied`，结构化空事实才为 `no_facts`
- [x] 正式表 before/after count/max 在恢复数据库上复核不变；facts/stores/batch/request/action/users/sessions/runs/artifacts 等逐类 residual=0 JSON
- [x] 数据库恢复后 MAX-002/003/004/006/007/008 repository 与 MAX-008 agenttools PG `-count=2` PASS、residual=0

## Browser / compatibility

- [x] 四页真实 browser viewport 1440×900 与 390×844 证据；四页移动 `scrollWidth=clientWidth=390`（R3 新增截图内容区像素差异已在 index 如实标记）
- [x] AI proposal 补充图已如实重命名 `r4-ai-proposal-1280x720.png`，索引标注 viewport 1280×720 / 位图 1269×714（非 1440×900 证据）
- [x] AI Chat mobile Drawer 冷加载无 hydration warning；active/non-active More 均可见；桌面仅当前 row hover/focus 显示 More；键盘/focus contract 通过
- [x] Console 无 uncaught/hydration/key/CORS；Recharts warning 已登记 Hardening
- [x] 同源关键 API race：真实同 tab 28→7 最终 DOM 与实际 route pattern 已记录；确定性 deferred gate 测试让新 7 天先提交、旧 28 天后提交被拒；浏览器 Network timing limitation 如实标注，见 `r5-race-gate.json`
- [x] `/`、`/performance`、`/contracts`、`/ai-chat`、`/upload`、`/monthly-closing`、`/reports`、`/audit-logs`、`/settings` smoke 正常
- [x] AppLayout、旧路由/导航/Skill/Tool、IFRS16/Official 未删除或替换

## 清理与回滚

- [x] scoped LE001/LE002 source/action/AI run/artifact：同一目标 ID 正反证据与精确 residual=0 已记录
- [x] residual JSON：R4 已加入 positive session `66fbe25c-e12e-46b1-b7d0-a02c6c74199e` / run `19dee11e-2090-4987-9416-c555b6255ea2` 及四个唯一 username/refresh 精确 selector，actual=0
- [x] 失败回滚：保留证据、状态回 `IN_PROGRESS/NO-GO`；恢复 Web 用 `docker compose build web && docker compose up -d web`

证据索引：[`evidence/MAX-009/index.md`](evidence/MAX-009/index.md)；报告：[`reports/MAX-009.md`](reports/MAX-009.md)。

## Review 5 收口

- `r5-race-gate.json`：页面实际消费的 request gate 与 deferred old-after-new commit 断言 PASS；浏览器控制面不能导出 HTTP timing，未伪造 status/结束顺序。
- `r5-negative-agent.json`：needs-input/no-facts/invalid-rate 的原始 assistant/event 直接含 reason、evidence、confidence；三例无 proposal/action 写入。
- `r5-residual.json`：R5 唯一用户、dataset、facts、stores、batch、AI runs/sessions/artifacts、actions 的精确 teardown 后逐类 `actual=0`。
- 定向 Go/Web 与 `pg_isready` 已 PASS；全量 Go/vet/Web/type-check/build/diff 作为最终提交门槛记录于报告。
- 最终全量：Go test/vet、Web 12 files/74 tests、type-check（顺序执行）、build、diff check PASS；repository PG 与 Agent tool PG `-count=2` 两轮 PASS。

## Review 6 收口

- [x] 删除 dataset_version `no-fact` 魔法命名分支；真实 scope denial 保持 `scope_denied`，结构化空事实才映射 `no_facts`。
- [x] 两条 Agent 语义路径的新增单测准确证明 deterministic Response/ProjectResult、SideEffects=false、proposal/artifact 均为 0；raw message_end 仅引用既有 R5 运行证据。
- [x] R4 `/api/v1/...` 已降级为历史无效网络记录/normalized Core endpoint；R5 gate 为当前有效 race 证据。
- [x] R4 positive session/run 已加入独立 exact residual selectors，actual=0。
- [x] Review 6 定向 Go test/vet、Web request-gate/type-check/build、pg_isready、diff check PASS；正式表 count/max 未变化。

## Review 7 收口

- [x] no-facts 单测改为普通法人 scope（`LegalEntityID=entity-a`，不设 Global/StoreIDs/region/brand），通过结构化空 population 得到 `no_facts`；另有不在授权 StoreIDs 内且 dataset 名含 `no-fact` 的测试保持 `scope_denied`。
- [x] 两例显式断言 `SideEffects=false`、`RetailActionProposal=nil`、`ProjectResult(...).Artifacts=0`。由于单测使用 `emit=nil`，本轮证据不声称新增 raw trace 或 action DB；raw message_end 继续引用 R5。
- [x] R4 四个唯一 username 的 users/refresh 依赖使用 exact `IN (...)` selector，已恢复的 B_LE001 UUID 另列，全部 residual=0；未臆造其余 UUID。
- [x] 任务票、报告、Evidence index、演示脚本与看板统一 `IN_REVIEW`，等待 Review 8；不进入 MAX-010。
