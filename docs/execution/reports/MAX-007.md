# MAX-007 交付报告：情景分析及行动草稿

状态：`ACCEPTED`  
日期：2026-08-12  
Executor：Max  
范围：仅 MAX-007；不进入 MAX-008。

## Review 2 收口记录

- PostgreSQL residual check 将 `retail_store_day_fact_requests.scope_key` 显式按 `text` 与法人参数比较，避免 varchar/UUID 操作符错误。
- PG fixture 使用全新 `scenario-pg-concurrent-first` key 进行两个首次并发保存，断言两个 200、同一真实 action ID、至少一个 `idempotent_replay`、数据库仅一行；新增 region/brand 不匹配拒绝及 B 法人保存 A 门店 404/零 action 断言。
- 过期结果卡的 horizon 标签改为读取 `response.horizon_months`，并增加纯逻辑测试，避免当前控件 horizon 覆盖旧结果标签。

Review 2 本地复现：定向 Go/Web、type-check、build 与 diff check 通过；真实 PostgreSQL `-count=2` 因当前环境未设置 `TEST_DATABASE_URL` 明确 skip，待 Reviewer 在 Docker 网络复现。状态保持 `IN_REVIEW`。

## Review 3 收口记录

PG action 断言已改为按 `scenario-pg-key` 与 `scenario-pg-key-v2` 精确查询：两者各一行、数据库 ID 与响应 ID 一致、两 ID 不同且保留 `Scenario action` / `Scenario action v2` 字段。并发 fixture 的第三行不再被模糊 scope 总数断言误判。定向 Repository 测试、Go vet、diff check 通过；真实 PostgreSQL `-count=2` 本地因 `TEST_DATABASE_URL` 未设置而两次明确 skip，待 Reviewer 在 Docker 网络复现。状态保持 `IN_REVIEW`。

## Review 1 收口记录

- Action persistence：新增场景专用数据库写入方法，`rule_code` 使用 fingerprint + Idempotency-Key 的稳定 bounded scope dedupe 值；`source_table=retail_store_day_facts`、`source_record_id=目标门店` 保持准确。`ON CONFLICT DO NOTHING` 后回读现有 idempotency 行，串行/并发同 key 同 payload 返回同一真实 ID；不同 payload 返回 409；新 key + 修改字段插入第二条真实行；法人范围隔离。未新增 migration，旧 `CreateAction` 和旧 action API 未改。
- Evaluate readiness：服务端在单次 `QueryFacts` 后调用 `retailkpi.AggregateFacts` 的 `retail-kpi-v1` current 语义，要求所有定义、覆盖、核心/辅助字段、零分母、质量和 mapping 均 decision-ready；不补零。
- 运行时降级测试覆盖 partial coverage、invalid、mapping failure、missing core/auxiliary、zero denominator、currency conflict 与 no facts，并逐项断言 422 reason/evidence。
- 422 evidence：`decision_not_ready`、`no_facts`、`incomplete_store_day_coverage`、`missing_required_field`、`zero_denominator`、`currency_conflict` 和 `resulting_rate_out_of_range` 返回最小 evidence（current、observed/expected、classification/dataset/source、fact versions/highest as-of 可得时、KPI drilldown URL）。resulting rate 越界为 422，结构/参数错误仍为 400。
- 页面 freshness：`evaluationSnapshotKey` 绑定门店/法人/模式/dataset/as-of/window/source、horizon 和七项 assumptions；任一变化使旧 response 失效并禁止保存。sequence gate 阻止旧 evaluate 覆盖新筛选；options 请求有 active guard、错误可重试；action retry 同 payload 复用 key，修改 action 字段生成新 key。
- 用户闭环：新增“证据与公式”Collapse；options 请求失败和合法空范围分开；成功卡展示真实 ID/status/Owner/Due/replay，并提供 `/performance` 链接。

## 1. 交付结果

- 新增 `/scenario-workbench`，沿用 `AppLayout`、`PageHeader`、Ant Design、既有 token/栅格/响应式；分析导航只新增“情景工作台”，位置在“门店 360”之后，既有导航名称、相对顺序、旧页面和旧入口保留。
- `/store-360` 保留“刷新”和“返回经营脉搏”，另加独立“情景分析”入口，完整传递 store、classification、dataset、as-of、window、source 和返回查询。
- 新增 `POST /api/v1/retail/stores/:store_id/scenarios/evaluate`（`reports:read`）与 `POST /api/v1/retail/stores/:store_id/scenario-action-drafts`（`fpna_actions:write`）。旧 `/reports/store-decision-scenario`、`/reports/store-decision-event-draft` 合同未改。
- 服务端从 `RetailKPIRepository.QueryFacts` 生成 30-day run-rate；客户端不能上传或覆盖 baseline。一次 evaluate 只调用一次 `QueryFacts`，不写事实/合同/事件/Official/IFRS16 表。
- 固定 `retail-store-scenario-v1` / `retail-kpi-v1` 版本，7 个显式 delta：revenue、gross margin pp、labor、fixed rent、variable rent rate pp、non-lease、other controllable cost。Baseline 固定全零，Plan 至少一个，输入顺序稳定。
- 金额与贡献桥最终按 half-away-from-zero 保留 2 位；百分比/百分比变化保留 4 位，`null` 与 reason 原样保留。
- action save 服务端重新 evaluate，只写现有 `fpna_action_items` 一行：`category=retail_store_scenario`、`status=open`、`verification_status=not_due`；Owner/Due 可空。fingerprint 包含法人、门店、日期窗口、classification/dataset/source、情景请求和行动字段；相同法人同 key 同 payload 返回 replay；同 key 不同 fingerprint 返回 409。

## 2. 手工 Golden 与确定性证据

手工 baseline：revenue 30,000、gross profit 9,000、gross margin 30%、labor 3,000、fixed rent 2,400、variable rent 300（1%）、non-lease 300、other 600、occupancy 3,000、contribution 2,400。

Plan：revenue +10%、gross margin +2pp、labor -5%、fixed rent -10%，其余为 0。服务纯逻辑测试断言：

- revenue 33,000；gross profit 10,560；labor 2,850；fixed rent 2,160；variable rent 330；non-lease 300；other 600；occupancy 2,790；contribution 4,320；
- monthly contribution change 1,920；12 个月 horizon 23,040；
- bridge：gross profit +1,560、labor +150、fixed rent +240、variable rent -30、non-lease 0、other 0；总和与月度变化一致，rounding residual 受控在 0.01；
- Store005 毛利率、Store006 人工、Store007 固定租金方向测试均断言正向贡献项；`revenue=-100%` 的 contribution margin 为 `null` + `zero_denominator`，不补 0；最终毛利率/变动租金率超界直接拒绝，不静默 clamp。

## 3. API、权限、来源与边界

- 参数只接受 window 7/14/28、horizon 3/6/12；pct `[-100,300]`、pp `[-100,100]`、有限数；unknown JSON field、重复 key、缺 baseline、baseline 非零均为 400。
- simulated 必须有 dataset version，production 禁止 dataset version；空法人/空门店/非法日期/非法 UUID 为 400；授权范围外门店 404；source conflict 409；事实不完整、无事实、覆盖不足、zero denominator、币种冲突为 422；Repository 失败为 500。
- response 明示 `basis=Scenario`、`side_effects=false`、`review_required=true`、`official_impact=false`、`ifrs16_impact=false`，并返回 store identity、classification/dataset/source、current window、currency、fact version、coverage、KPI drilldown URL 与请求假设。
- 经营占用现金成本只来自 fixed rent + variable rent + non-lease；代码、页面和 action evidence 均没有 NPV、IFRS16、ROU、租赁负债、市场租金、选址、搬迁、房东或最优方案字段。
- action evidence 带目标门店、当前窗口、source/dataset/fact versions、scenario/formula versions 和服务端结果；不会写 `lease_contracts`、`measurement_results`、`journal_entries`、事件或 Agent 表。

## 4. 自动化命令原文与结果

以下命令均在仓库根目录或标注目录执行：

```text
cd core-service && GOCACHE=$(pwd)/.gocache GOMODCACHE=$(pwd)/.gomodcache go test ./...
```

结果：MAX-007 相关包通过（`internal/services/retailscenario`、handlers、repository 及其依赖）；仓库全量命令在既有 `internal/aiagent/TestParsePaymentScheduleUsesVersionedIntakeContract` 创建 `httptest.NewServer` 时因本机沙箱禁止绑定 IPv6 临时端口而 panic（`listen tcp6 [::1]:0: bind: operation not permitted`），该环境阻塞与本票无关，未伪报为全量通过。

MAX-007 定向复核命令：

```text
cd core-service && GOCACHE=$(pwd)/.gocache GOMODCACHE=$(pwd)/.gomodcache \
go test ./internal/services/retailscenario ./internal/handlers ./internal/repository
```

结果：PASS。

```text
cd core-service && GOCACHE=$(pwd)/.gocache GOMODCACHE=$(pwd)/.gomodcache go vet ./...
```

结果：PASS。

```text
cd web && npm test -- --run && npm run type-check && npm run build
```

结果：PASS；9 个 test files、55 tests；`type-check` 与 Next.js production build 成功，并生成 `/scenario-workbench`。Review 2 新增的 horizon formatter 纯逻辑测试通过。

```text
git diff --check
git diff --name-only -- web/app/page.tsx web/app/performance/page.tsx
```

结果：PASS；旧首页和 `/performance` 没有本票 diff；本票未添加 migration，未改旧情景 API 合同。

## 5. PostgreSQL 集成证据

执行命令：

```text
cd core-service && GOCACHE=$(pwd)/.gocache GOMODCACHE=$(pwd)/.gomodcache \
go test -v ./internal/repository \
-run 'TestRetailKPIPostgresGoldenVersionSourceCurrencyAndTenantBoundaries|TestRetailPulsePostgresGoldenAffectsAndIsolation|TestRetailScenarioPostgresGoldenIsolationAndZeroTouch|TestRetailStoreDiagnosticsPostgresGoldenAndIsolation' \
-count=2
```

结果：命令退出 PASS，但本执行环境 `TEST_DATABASE_URL` 未设置，四项真实 PostgreSQL 测试各自两次均为 `SKIP: TEST_DATABASE_URL is not set`。没有申请权限、没有启动临时 PostgreSQL，也没有把 skip 记作真实 PG 通过。

新增的 `TestRetailScenarioPostgresGoldenIsolationAndZeroTouch` 已编译，Reviewer 可在既有 Docker 网络运行。测试覆盖：

- A/B simulated dataset、Store005 目标、source/dataset/fact version provenance；
- production 门店 current 28 天完整事实且 `dataset_version` 为空；
- A/B 法人交叉门店返回 `ErrStoreNotFound`；
- Repository counting DB 固定 2 个 SQL Query、无 per-store N+1；
- action 首次写入、同 key replay、不同 payload 409、最终只保留一行 `open retail_store_scenario`；
- `lease_contracts`、`measurement_results`、`journal_entries` 和原 production facts zero-touch；cleanup 顺序先 action/facts/dataset/batch/stores/entity，重复运行无残留。

## 6. 浏览器与风险

本轮没有尝试本地浏览器端口，也没有伪造截图；浏览器本地 URL 仍是外部权限阻塞，保留为 MAX-009 发布前置验收。页面静态 production build 和纯逻辑测试已通过，Reviewer 仍需在可用浏览器完成 1440×900 与 390×844 交互验收。

剩余风险仅为真实 PostgreSQL 需 Reviewer 在 Docker 网络复现；本地环境未对数据库状态做任何写入。

## 7. Reviewer 复现步骤

1. 在现有 Docker 网络设置 `TEST_DATABASE_URL=postgres://lease:lease_secret@lease-postgres:5432/lease?sslmode=disable`，从仓库根目录执行上面的 `go test ... -count=2` 命令。
2. 检查两次 `TestRetailScenarioPostgresGoldenIsolationAndZeroTouch` 日志中的 SQL count、production/simulated provenance、action replay/409、zero-touch 与 cleanup；确认 `SCENARIO-LE-*`、`scenario-pg-key` 和相关 dataset/facts 为 0 残留。
3. 运行 `cd web && npm test -- --run && npm run type-check && npm run build`，打开 `/scenario-workbench`，从 `/store-360` 进入并验证 URL round-trip、7/14/28、production/simulated dataset 互斥、空门店空状态、Baseline/Plan、贡献桥和 action 草稿。
4. 用已有账号分别验证 A/B 法人，确认 simulated latest/source/version 和 production Working 信封不会串租户、不会进入 Official/IFRS16。

## Review 1 PostgreSQL 复现状态

本轮新增/修正的真实测试先创建 production fixture，再拍 zero-touch 快照；快照覆盖 `lease_contracts`、`measurement_results`、`journal_entries`、`monthly_closing_batches`、Official `fpna_report_artifacts` 和 production facts。测试随后覆盖：

- region、brand、store scope；
- A/B 法人同 key action 隔离；
- 同 key 串行 replay、并发双击同真实 ID、同 key 异 payload 409；
- 新 key + 修改 title 生成第二真实行；
- action 的金额、evidence fingerprint、Owner、Due、status=open、category；
- fixture 后的 Evaluate/Save zero-touch 与 cleanup 后法人、门店、facts、datasets、actions、batches 无残留。

复现命令原文：

```text
cd core-service && GOCACHE=$(pwd)/.gocache GOMODCACHE=$(pwd)/.gomodcache \
go test -v ./internal/repository \
-run '^TestRetailScenarioPostgresGoldenIsolationAndZeroTouch$' -count=2
```

本 Executor 环境仍未设置 `TEST_DATABASE_URL`，因此本地两次均为 `SKIP: TEST_DATABASE_URL is not set`，没有声称真实 PostgreSQL 已通过。Reviewer 应在既有 Docker 网络执行上面命令，并再运行 MAX-003/004/006 相关测试 `-count=2`；以 Docker 输出的 query count、耗时和零残留为最终真实库证据。
