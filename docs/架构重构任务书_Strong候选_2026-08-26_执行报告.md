# 架构重构任务书执行报告 · Strong 三候选（2026-08-26）

> 对应任务：[架构重构任务书_Strong候选_2026-08-26.md](架构重构任务书_Strong候选_2026-08-26.md)。
> 执行顺序：C2 → C3 → C1，每步全绿才进下一步；三个候选共用一条工具面基线。

## 共同基线（先于一切动刀）

`internal/aiagent/tool_inventory_golden_test.go`：用 `productionWire()` 构造生产配置 registry，
`Runtime.Describe` 全量清单落盘 `internal/aiagent/testdata/tool-inventory.json`
（**61 个工具**，含 name/version/level/permissions，按名排序、逐字节比对）。
再生需显式 `UPDATE_TOOL_INVENTORY=1`。三个候选完工后该快照均逐字节一致——
期间它真实拦截过一次事故（见 C2 红线记录）。

## C2 · 合并签约前测算三包 —— ✅ 完成

**锚（先钉后动）**：
- HTTP 层 golden：`handlers/deals_golden_test.go` 钉住 `POST /deals/briefing` 与
  `POST /deals/compare` 的完整响应（固定请求体 → 逐字节比对，键序即契约）；
- 服务层 golden：renttosales 的 HTTP 层依赖具体 PG 仓储指针，无法无库起 HTTP，
  其锚放在 `ratio_golden_test.go`（四家门店覆盖 healthy/watch/over/no_revenue
  四条路径 + 组合比 + 覆盖声明，Result JSON 逐字节）；
- 工具清单基线 + `agenttools/tools` / `agentseval` / `workingpaper/s1` /
  `renewaldecision` 既有测试全绿确认后才动刀。

**纯移动**：三包 6 文件 + 测试 + testdata `git mv` 进
`internal/services/leasescenario/`，包注释随迁。两处**机械性**调整（合并必然产生，
零逻辑改动）：
- `dealcompare.Input` → `CompareInput`、`renttosales.Input` → `RatioInput`（同名冲突）；
- 三份完全相同的 `round2` 去重为一份。

**接缝**：合并后的包函数面（`Build` / `Compare` / `Calculate`）即统一 Engine 接缝。
未再包一层单实现 Go interface——这三个计算是纯函数、没有可替换的后端，
ControlReaders 先例解决的是「有仓储要 fake」的问题，这里不存在；GUARD-001 由编译器
承担（见下）。此偏离已在任务内注明理由。

**删除**：`ls internal/services/{predeal,dealcompare,renttosales}` 均不存在；
全仓 grep `services/(predeal|dealcompare|renttosales)` 零命中。

**红线记录（golden 的价值实证）**：搬家时批量替换 import 把字符串字面量
`"lease.predeal.simulate"` 误改成了 `"lease.leasescenario.simulate"`——工具名变了，
**工具清单 golden 立刻红**，定位到 `tool_bundles` 前身与 `workingpaper/s1.go` 的
`EnginePredeal` 两处并还原。这正是任务书「工具枚举数量变化 = 停」要防的事故。

**GUARD-001 实测**：给 `leasescenario.Build` 加一个参数 → `go build ./...` 在
renewaldecision、s1、agentseval 等 5 个消费包编译红；还原后恢复绿。

## C3 · IngestBatch 摄取接缝 —— ✅ 完成

**锚**：`make test-integration ARGS="... -run StoreDay -v"` 真跑（一次性容器，
非 SKIP），开工与完工各一次，输出一致：

```
TestRetailStoreDayFactsPostgresConstraintsIdempotencyAndTenantScope   PASS (RUN)
TestRetailStoreDayFactsPostgresAtomicAuditIdempotencyAndRollback      PASS (RUN)
TestRetailStoreDayFactsPostgresSchemaHasLookupIndex                   PASS (RUN)
TestRetailStoreDayFactsPostgresPaginationHasReliableTotal             PASS (RUN)
TestRetailStoreDayFactsPostgresAuditDimensionScope ×3                 PASS (RUN)
TestRetailStoreDayFactsTenantIsolationByIDsCharacterization           PASS (RUN)
```

另加 HTTP 层 golden：`retail_ingest_golden_test.go` 用既有 fake 钉住
preview / commit / **幂等重放**三条响应（挂钟字段 scrub 后逐字节）。

**立接缝**（`services/retailingest` 包内新增，未改任何既有签名）：
- `BatchStorer` 端口（CreateBatch/FinalizeBatch）+ `Service.WithBatchStore`；
- `PreviewBatch(ctx, PreviewSpec)`：解析 → 建议（人确认优先）→ 门店归属 → 校验 → 覆盖估算；
- `IngestBatch(ctx, IngestSpec)`：解析 → 映射确认 → 归属 → 建批次行 → 幂等重放短路 →
  envelope 三元组校验 → 分块原子落库 → 收尾批次行，顺序与原 handler 逐步一致；
- `ImportError{Kind, Message, Err, BatchID, Report}` 让错误→状态码的翻译留在 handler
  一处完成，且原文案逐字保留（parse/envelope → 400 invalid_arguments；no_valid_rows →
  422 + report/batch_id details；commit 失败走 writeCodedFailure 透传原始错误码映射）。

**收 handler**：`retail_ingest.go` 从 327 行编排体收敛为参数解析 + 端口接线 +
错误翻译；ParseTemplate/SuggestMapping/ResolveStores/Validate/CreateBatch/
FinalizeBatch/Commit 等编排调用在 handler 中归零。已知微差（已注明）：tenantEntity
的 403 检查从「映射 JSON 解析之后」提前到进引擎之前——仅当租户范围配置损坏与映射
JSON 损坏同时发生时才有可见差异，前者是认证面故障，提前失败更安全。

**验收对照**：幂等/原子性集成测试对真库 RUN 过（上表）；构造中途失败零部分行由
AtomicAuditIdempotencyAndRollback 锚定；preview/commit/replay golden 一致；
envelope 三元组 + version/data_classification 在 `Service.Commit` 内原样产出（代码未动）。

## C1 · 拆解 Agent 单体 —— ✅ 完成（步骤 1–4）

1. **记基线**：见共同基线（61 工具落盘）。
2. **Tool Provider**：注册面拆成 `registerLeaseTools` / `registerFPnATools` /
   `registerRetailTools`（`internal/aiagent/tool_bundles.go`，230 行），归属以
   **工具名命名空间**判定——跨域端口如实标注（如 `lease.portfolio.summary` 挂在
   经营事实端口上、`lease.renewal.decisions` 来自 controls.Renewal）。原 if-nil
   守卫逐条保留（P0-8 的 finModelRepo 二选一分支、performance 的 writer 类型断言、
   storePnl 缺席即不注册）。fail-fast `registerCollector` 未动一字。
3. **抽纯函数**：结果投影群（`ProjectResult` 及 artifact/复核理由推导）迁入
   `result_projector.go`，上下文装配群（AR3 接缝使用侧）迁入 `context_assembly.go`。
   同包文件级搬家，零逻辑改动。
4. **收敛构造函数**：17 固定参 + variadic → `AgentPorts` 分组结构（15 个角色字段）；
   合并接口 `OperatingFactsSource`（PerformanceReader + OperatingFactsReader）与
   `RetailKPISource`（RetailOperationsReader + FactsSource）让 main.go 的三个重复
   仓库**各只传一次**；`NewWithOperationalReadersAndGovernanceAndRetail` 删除，
   新构造器 `NewAgent(ports)`；调用链 main.go → handlers.NewAIChatHandler(runtimeRepo,
   contractRepo, ports) → aiagent.NewAgent(ports) 同步更新。With* builder 续用，未建新机制。
   agent.go 3205 → 2729 行（行数不是 KPI，接缝才是——拆出的是 bundle 与两个纯函数群）。

**验收对照**：
- Runtime.Describe 清单与基线 diff 为空（61 = 61，逐字节）✅
- `registration_completeness_test.go` 断言未改、绿（61 attempted == succeeded == Describe）✅；
  `governance/dualpath_rc1_test.go` 原样绿（治理链一行未碰）✅
- main.go 无重复传参 ✅
- Planner/Executor 独立单测：`aichat/planner_executor_independence_test.go`——坏 planner
  panic 时 executor 不被触达；健康 planner + 坏 executor 时 run 标记 failed 且 planner
  已被咨询。任一侧改坏对应测试独立变红 ✅

## 最终验证

```bash
cd core-service && GOCACHE=$(pwd)/.gocache go test ./... && go vet ./...   # ✅ 全绿
make test-integration ARGS="./internal/repository/ -run StoreDay"          # ✅ 6 函数 RUN 非 SKIP
cd ../web && npm run type-check && npm run build && npm test               # ✅ 本轮 web 零改动（上一任务已全绿）
```

## 遗留与建议

- `dealcompare/compare_test.go` 里约 90 个链式同义格式化 helper（formatGap→…→fin5）
  是存量测试噪音，本次按「不改一行逻辑」未动，建议测试债专项清理。
- `retailingest_test.go`、部分 handlers 文件存在 pre-existing gofmt 漂移，本轮未顺手修
  （避免污染行为保持型 diff）。
- T4 报告中的 scope_denied 结构性违规（六个 seam 页第四态被静默）仍是下一个 UIUX 专项。
